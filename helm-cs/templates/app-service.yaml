apiVersion: v1
kind: Service
metadata:
  name: skillexchange-active-service
 
spec:
  selector:
    app: skillexchange-app
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
 
---
apiVersion: v1
kind: Service
metadata:
  name: skillexchange-preview-service
spec:
  selector:
    app: skillexchange-app
  ports:
    - port: 3000
      targetPort: 3000
  type: ClusterIP
 
 